#!/bin/sh

. ../OPTIONS.sh

#RSYNCSOURCE=rsync://download.fedora.redhat.com/fedora-enchilada
RSYNCSOURCE=rsync://mirrors.kernel.org/fedora-enchilada
BASEDIR=${MIRRORDIR}/fedora

rsync "$@" -vaH --numeric-ids --delete --delete-after --delay-updates \
  ${RSYNCSOURCE} ${BASEDIR}
