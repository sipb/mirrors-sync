#!/bin/sh

RSYNCSOURCE=ftp6.us.FreeBSD.org::FreeBSD/ 
BASEDIR=${MIRRORDIR}/FreeBSD

rsync --recursive --times --links --hard-links \
      --stats \
      --verbose \
      --perms \
      -D \
      --delete \
      --timeout=300 \
	--size-only -z \
      ${RSYNCSOURCE} ${BASEDIR}
